<div class="journal_edit__btn">
  <%= link_to '＜ 戻る', root_path, class: 'btn' %>
  <%= link_to '削除 ＞',"/journals/#{@journal.id}", data: {turbo_method: :delete}, class: 'btn' %>
</div>
<%= render 'form', journal: @journal %>
